import '../mdx.css';

<div className="sb-container">
<div>
# Usage

```jsx
import { Popover, usePopover } from './lib/components/Popover';

function App() {
  const divRef = useRef < ElementRef < 'div' >> null;
  const { onOpen: onOpenPopover1, ...popoverProps1 } = usePopover();

  return (
    <>
      <button onClick={onOpenPopover1}>Open Popover</button>
      <Popover {...popoverProps1} placement='bottom-start' dismissDivs={[divRef.current]}>
        content popover
      </Popover>
      <div
        ref={divRef}
        className='grid place-items-center bg-background-scrim z-modal fixed bottom-0'
      >
        123123123
      </div>
    </>
  );
}
```

# API

Component accepts 7 arguments:

<ul>
  <li>
    <code>dismissDivs</code> – array of nodes that should not trigger outside click event
  </li>
</ul>

</div>

<div className="sb-table__content">
<ul>
<li>[Usage](#usage) </li>
<li>[API](#api)</li>
</ul>
</div>
</div>
